import { Component, Input } from "@angular/core";

@Component({
    selector: "Header",
    templateUrl: "./Header.html",
    styleUrls: [
        "./Header.css"
    ]
})

export class Header { 
    public image: string = "./"
}